# bounce-bounce
just a little bouncing ball simulation. Implemented in python and uses pygame for rendering/game physics.

This version allows for multiple balls to be spawned when the user clicks on screen